name: ubuntu

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['[0-9]+.[0-9]+.[0-9]+']
  pull_request:
    types: [opened, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ubuntu-build:
    runs-on: ${{ matrix.ubuntu-runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - ubuntu-runner: ubuntu-22.04
            arch: x86_64
          - ubuntu-runner: ubuntu-22.04-arm
            arch: aarch64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: latest
          cache: true
          cache-write: ${{ github.event_name != 'pull_request' }}

      - name: Cache conan packages
        uses: actions/cache@v4
        with:
          path: ~/.conan2/p
          key: conan-ubuntu-${{ matrix.arch }}-${{ hashFiles('conanfile.py') }}
          restore-keys: conan-ubuntu-${{ matrix.arch }}-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-ubuntu-${{ matrix.arch }}-${{ github.sha }}
          restore-keys: ccache-ubuntu-${{ matrix.arch }}-

      - name: Build & Test
        run: pixi run all

      - name: Package artifacts
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          mkdir -p dist
          find build/Release -name "*.so" -exec cp {} dist/ \; 2>/dev/null || true
          ls -la dist/
          tar -czf pj-test-dummy-plugins-${{ github.ref_name }}-linux-${{ matrix.arch }}.tar.gz -C dist .

      - name: Upload artifact
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: pj-test-dummy-plugins-${{ github.ref_name }}-linux-${{ matrix.arch }}
          path: pj-test-dummy-plugins-${{ github.ref_name }}-linux-${{ matrix.arch }}.tar.gz

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: pj-test-dummy-plugins-${{ github.ref_name }}-linux-${{ matrix.arch }}.tar.gz
          generate_release_notes: true
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
